// loggingUtils.ts

export const logMessage = (message: string): void => {
  console.log(message);
};

export const logError = (error: Error): void => {
  console.error(error);
};

// 추가적인 로깅 함수들을 여기에 정의할 수 있습니다.
